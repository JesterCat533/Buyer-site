{
  "name": "custom-buying-server",
  "version": "1.0.0",
  "description": "Backend for handling Stripe payments and Discord webhooks.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^15.12.0"
  }
}
